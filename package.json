{
  "name": "efgc-youth-mobile",
  "private": true,
  "version": "0.1.0",
  "description": "Capacitor Android packaging for the EFGC Youth static site",
  "scripts": {
    "mobile:prepare": "node scripts/prepare-mobile-web.mjs",
    "mobile:android": "npm run mobile:prepare && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
