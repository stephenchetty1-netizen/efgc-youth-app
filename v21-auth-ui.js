(()=>{let pending=null,$=s=>document.querySelector(s),msg=t=>$('#loginMessage').textContent=t;window.loginUser=async()=>{const d={name:$('#loginName').value.trim(),phone:$('#loginPhone').value.trim(),email:$('#loginEmail')?.value.trim()||'',dob:$('#loginDob').value,role:loginRole};if(!d.name||!d.phone)return msg('Name and cellphone number are required.');if(d.role==='admin'&&!d.email)return msg('Admin email is required.');if(d.role!=='admin'&&(!d.dob||!$('#loginPhoto').files.length))return msg('Birthday and a face photo are required.');if(d.role==='youth'&&(!$('#parentName').value.trim()||!$('#parentPhone').value.trim()||!$('#emergencyName').value.trim()||!$('#emergencyPhone').value.trim()))return msg('Parent / guardian and emergency contact details are required.');try{if(d.role==='admin'){d.email=await EFGCAuth.requestEmailOtp(d.email)}else{d.phone=await EFGCAuth.requestPhoneOtp(d.phone)}pending=d;let box=$('#supabaseOtpBox');if(!box){box=document.createElement('div');box.id='supabaseOtpBox';box.className='otp-box';box.innerHTML='<label><span id="otpLabel">Verification code</span><input id="supabaseOtp" inputmode="numeric" maxlength="10" placeholder="Enter OTP"></label><button class="primary-login" type="button" onclick="verifySupabaseOtp()">Verify & Continue</button>';$('#loginMessage').before(box)}$('#otpLabel').textContent=d.role==='admin'?'Email verification code':'SMS verification code';msg(d.role==='admin'?'Verification code requested. Check your email and enter the code to continue.':'Verification code requested. Enter the SMS code to continue.')}catch(e){msg(`Secure sign-in could not start: ${e.message}`)}};window.verifySupabaseOtp=async()=>{if(!pending)return msg('Request a verification code first.');const token=$('#supabaseOtp').value.trim();if(!/^\d{6,10}$/.test(token))return msg('Enter the verification code.');try{const a=pending.role==='admin'?await EFGCAuth.verifyEmailOtp(pending.email,token):await EFGCAuth.verifyPhoneOtp(pending.phone,token),requested=pending.role==='admin'?'youth':pending.role,approval=requested==='leader'?'pending':'approved';await EFGCAuth.upsertProfile({full_name:pending.name,phone:EFGCAuth.normalizeZA(pending.phone),birthday:pending.dob||null,face_photo_path:null,role:requested,approval_status:approval,leader_role:requested==='leader'?($('#loginRoleText').value.trim()||'EFGC Youth Leader'):null});if(requested==='youth'&&pending.role!=='admin')await EFGCAuth.upsertSafeguarding({parent_name:$('#parentName').value.trim(),parent_phone:$('#parentPhone').value.trim(),emergency_name:$('#emergencyName').value.trim(),emergency_phone:$('#emergencyPhone').value.trim()});session={name:pending.name,phone:pending.phone,role:requested,uid:a.user.id};localStorage.setItem('efgcYouthSession',JSON.stringify(session));if(pending.role==='admin')msg('Identity verified. This account is awaiting the secure first-Admin promotion.');else if(requested==='leader')msg('Leader application submitted. Admin approval is required.');else{hideLogin();render()}}catch(e){msg(`Verification failed: ${e.message}`)}};window.logoutUser=async()=>{await EFGCAuth.signOut();session=null;localStorage.removeItem('efgcYouthSession');showLogin()}})();